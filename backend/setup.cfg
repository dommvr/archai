[flake8]
# Line length aligned with Black's default (88) plus a small buffer.
# Docstrings and log messages in this codebase regularly exceed 79 chars.
max-line-length = 119
# E501: line too long — handled by max-line-length above
# W503: line break before binary operator — conflicts with Black
extend-ignore = W503
